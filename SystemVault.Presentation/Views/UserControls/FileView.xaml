<UserControl x:Class="SystemVault.Presentation.Views.UserControls.FileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SystemVault.Presentation.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            
            <Grid Grid.Column="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200" />
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Margin="5">
                    <Label Padding="0">Name</Label>
                    <TextBox x:Name="txbName" />
                </StackPanel>
                
            </Grid>

            <Grid Grid.Column="1">
                <StackPanel VerticalAlignment="Center">
                    <Button Content="Search" Click="SearchButton_Click" />
                    <Button Content="Reset" Click="ResetFiltersButton_Click" />
                </StackPanel>
            </Grid>
        </Grid>

        <Grid Grid.Row="1">
            <Button Content="Add" HorizontalAlignment="Right" VerticalAlignment="Center" Click="AddServiceFileButton_Click" />
        </Grid>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            
            <DataGrid Grid.Row="0" x:Name="dtg" IsReadOnly="True" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Width="*" Binding="{Binding Name}" />
                    <DataGridTextColumn Header="Path" Width="*" Binding="{Binding Path}" />
                    <DataGridTextColumn Header="Size" Width="*" Binding="{Binding Size}" />
                    <DataGridTextColumn Header="CreatedAt" Width="*" Binding="{Binding CreatedAt, StringFormat={}{0:dd.MM.yyyy HH:mm:ss}}" />
                    <DataGridTextColumn Header="Category" Width="*" Binding="{Binding Category.Name}" />
                </DataGrid.Columns>
            </DataGrid>

            <Grid Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Top">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <ComboBox Name="cmbPageSize" Grid.Column="0" Margin="5" Height="30" Width="70" SelectionChanged="PageSizeComboBox_SelectionChanged">
                    <ComboBoxItem Content="10" Tag="10" />
                    <ComboBoxItem Content="20" Tag="20" />
                    <ComboBoxItem Content="50" Tag="50" />
                    <ComboBoxItem Content="100" Tag="100" />
                </ComboBox>
                <Button Grid.Column="1" Margin="5" Height="30" Width="50" Click="PrevButton_Click">Previous</Button>
                <Button Grid.Column="2" Margin="5" Height="30" Width="50" Click="NextButton_Click">Next</Button>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
